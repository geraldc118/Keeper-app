import React from "react";
import ReactDom from "react-dom";
import App from "./components /App.jsx"


ReactDom.render(<App />,document.getElementById("root")
);



// Use this as a template for creating React apps.
// Make sure that the components are wrapped in () when using the return as items may not show sometimes
